# 2026-04-14 Worklog

## v1.0.117 - Agent 联网工具注册
- 新建 `lib/services/web/web_agent_tools.dart` - WebSearchAgentTool + WebFetchAgentTool
- AppState 注册 web 工具：`registerWebTools(_agentLoopV2, _webSearchService, _webFetchService)`
- 之前 Agent Loop 里有 WebSearchService 和 WebFetchService，但从来没注册为 Agent Tool
- 现在用户说"帮我查天气"或"搜索xxx"，Agent 能调用 web_search 工具了
- 构建成功 71.9MB

## v1.0.118 - Agent 触发优化 + 计算器 + Web引导
- 新增 CalculatorTool - 递归下降解析器，支持 + - * / ** () 和 π、e
- 精简 Agent 触发正则：移除"分析/调研/整理/规划/制定"等太宽泛的关键词
- Agent system prompt 动态添加 web_search 联网搜索使用指南
- 构建成功 71.9MB

## v1.0.119 - 消息时间戳 + 分享功能 + 菜单优化
- 每条消息显示智能时间戳（刚刚/X分钟前/HH:MM等）
- 长按菜单分享功能接入系统分享（share_plus → 微信/QQ等）
- 长消息在菜单中显示内容预览
- 构建成功 72.0MB

## v1.0.120 - 超时保护 + Agent 卡死修复 + Skill 降级优化（重大修复）
- **修复 Agent 跑半小时不出来的问题**：所有 LLM 调用加超时保护（chat 60s / stream 120s）
- Agent Loop 整体 3 分钟超时强制终止
- Skill 失败降级到 Agent 时强制单步模式（不走 TaskDecomposer）
- 添加文本格式工具调用解析回退（LLM 不支持 function calling 时）
- Agent 工具调用 UI 实时回调（onToolCall/onToolResult）
- 合并 v1.0.119 的消息时间戳和分享功能
- 构建成功 72.0MB

### 用户反馈（v1.0.117 录屏问题）
1. 版本号没更新（还是116）→ 可能 APK 没装上
2. 跑了半小时没出来 → LLM 无超时 + Agent 卡死 → **已修复**
3. 不会用 skill → 天气被 TaskDecomposer 拆成6步 → **已修复（单步降级）**

### 下一步方向
- IntentRecognizer 硬编码问题 - _availableSkills 应该从 SkillManager 动态获取
- Agent 触发可以更智能 - 用 LLM 判断是否需要工具，而不是纯正则
- 改进 DuckDuckGo 搜索结果质量

