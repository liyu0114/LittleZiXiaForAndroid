# 2026-04-12 Worklog

## v1.0.112 - 智能上下文管理
- 新建 `SmartContextService` - 对话历史自动压缩 + 跨话题记忆搜索 + Token感知裁剪
- 集成到 AppState
- 修复版本号不一致（3处统一）
- 修复 PowerShell Set-Content 破坏中文编码

## v1.0.113 - Skill降级自主解决 + 群聊LLM改进
- Skill 不可用时降级到 Agent Loop（用 web_search 等工具）
- 群聊 `_tryExecuteSkill` 天气 Skill 失败后回退到 web_search
- 群聊 `generateLLMResponse` 添加 System Prompt

## v1.0.114 - 沙盒代码编辑 + 对话改代码
- `code_preview_screen.dart` 重写：
  - 代码 tab 新增编辑按钮 → TextField 编辑模式
  - 新增💬按钮 → 跳转主聊天，自动填充项目代码
  - 底部提示条引导用户
- AppState 新增 `pendingMessage` 机制
- `home_screen.dart` initState 检查 pendingMessage
- 构建成功 71.9MB

### 教训
- PowerShell 的 Set-Content 会破坏 UTF-8 中文编码
- 版本号需要在三个地方同步：pubspec.yaml、app_version.dart、settings_screen.dart
- Flutter `Tee-Object` 可以同时输出到文件和控制台

### 下一步方向
- 沙盒创建的项目应该生成实际有用的代码（不只是模板）
- Agent Loop 需要更好的 system prompt 指导它用代码工具
- 对话改代码的完整闭环测试
