# 开发铁律 — 每次动手前必读

> **最大原则：只写通用机制，不针对单个功能硬编码**

---

## 一、思维方式检查表（动手前过一遍）

1. **这个改动是通用的还是针对某个具体场景？**
   - ❌ "给 location 加 builtin 支持" → 这是硬编码
   - ✅ "让 SkillExecutor 支持所有 builtin 指令" → 这是通用机制

2. **这个问题本质是什么？**
   - ❌ "天气查不了" → 表面现象
   - ✅ "Agent 缺少工具时不会自主发现和安装" → 本质问题

3. **修复方案是否只解决一个 case？**
   - 如果只解决天气，那翻译、计算、导航等问题还是会有
   - 必须找到能覆盖所有类似场景的通用方案

---

## 二、小紫霞的核心架构思路

```
用户提问
  ↓
Agent Loop（LLM + 工具调用循环）
  ↓
有工具？→ 调用工具 → 看结果 → 继续或完成
没有工具？→ 自动搜索 SkillHub → 安装 → 再调用
```

**所有功能都通过工具（Tool）暴露给 Agent。** 不存在"硬编码某个技能的特殊路径"。

---

## 三、通用机制 vs 硬编码

| 场景 | ❌ 硬编码做法 | ✅ 通用机制做法 |
|------|-------------|---------------|
| 缺少天气技能 | 在代码里写死天气API调用 | Agent 自动发现缺技能→搜索SkillHub→安装→使用 |
| 需要位置信息 | 在 SkillExecutor 里加 location builtin | Agent 自主调用已有的位置工具或搜索安装 |
| 新模型不支持FC | 针对某个模型适配 | 通用文本解析回退（已有） |
| 技能加载失败 | 针对某个 SKILL.md 修 | 修复通用的 SkillLoader |

---

## 四、当前已知问题（按优先级）

### P0：Agent 不主动获取缺失能力
**现象：** 用户问"我这天气"，Agent 直接问用户要城市名
**本质：** LLM 没有调用工具就直接文字回复时，Agent Loop 就认为完成了
**通用方案：**
- 检测 LLM 是否在"向用户要信息"而不是用工具
- 如果是，注入提示让 LLM 去 SkillHub 搜索安装缺失的技能
- v1.0.125 已实现 `_looksLikeGivingUp` 检测

### P1：Skills 加载为0
**现象：** "技能: 0个"
**本质：** pubspec.yaml 中注释和路径写在同一行，5个技能被注释掉
**但：** 即使5个被注释，剩余的也应该能加载，0个说明还有其他问题
**状态：** pubspec.yaml 已修复，需验证

### P2：SkillExecutor 只支持 http/curl
**现象：** 含 dart 代码块的技能无法执行
**本质：** SkillExecutor 只认 http/bash/builtin 三种代码块
**通用方案：** 这是架构限制，但 Agent 可以用 run_script 或 web_search 作为替代

---

## 五、开发流程（每次必走）

1. **先看这份文档** → 确认思路方向正确
2. **先看 TASK_STATUS.md** → 了解当前进展
3. **找到问题的本质** → 不看表面现象
4. **只写通用方案** → 不硬编码
5. **改完后更新这份文档** → 记录决策和教训

---

## 六、历史教训

### 2026-04-17：反复横跳事件
- 问题：用户反馈 Agent 不会任务分解（以天气为例）
- 错误做法1：给 location 加 builtin 硬编码
- 错误做法2：改 SKILL.md 格式适配单个技能
- 正确做法：让 Agent 在缺少工具时自动搜索 SkillHub
- **教训：天气只是例子，用户要的是通用的自我补能力机制**
